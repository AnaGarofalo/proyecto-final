#Configuraci�n bases de datos
database.name.rag=proyectofinal_rag
database.port=5432
database.domain=localhost
database.name=proyectofinal_test
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.show-sql=true
spring.datasource.url=jdbc:postgresql://${database.domain}:${database.port}/${database.name}?useSSL=false&serverTimezone=UTC
spring.datasource.username=${DATABASE_USERNAME:proyectofinal}
spring.datasource.password=${DATABASE_PASSWORD:proyectofinal}

valid.email.domains=random-domain.com,other-domain.com