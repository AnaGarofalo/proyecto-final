server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/proyectofinal?useSSL=false&serverTimezone=UTC
    username: root
    password: proyectofinal
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE